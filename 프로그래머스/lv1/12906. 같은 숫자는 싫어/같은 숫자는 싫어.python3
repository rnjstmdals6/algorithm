def solution(arr):
    before = arr[0]
    result = [arr[0]]
    for i in range(1, len(arr)):
        if before == arr[i]:
            continue
        else:
            before = arr[i]
            result.append(arr[i])
    return result